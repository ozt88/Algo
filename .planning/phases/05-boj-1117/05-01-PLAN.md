---
phase: 05-boj-1117
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - main.go
  - main_test.go
  - history/2026-W14-BOJ-1117.md
autonomous: true
requirements:
  - PROB-01
  - WF-01
  - WF-02
  - WF-03
  - WF-04
  - CODE-01
  - CODE-02
  - CODE-03
  - LOG-01
  - LOG-02

must_haves:
  truths:
    - "main.go가 BOJ 1117 입력을 읽고 색칠되지 않은 면적을 출력한다"
    - "x축 접기에서 겹치는 구간을 올바르게 2배 처리한다"
    - "y축 접기에서 (c+1)배 곱셈을 올바르게 수행한다"
    - "int64로 오버플로 없이 계산한다"
    - "main_test.go에 최소 6개 테스트 케이스가 통과한다"
    - "history 아카이브가 존재한다"
  artifacts:
    - path: "main.go"
      provides: "BOJ 1117 풀이 (parseInput + solve)"
      contains: "func solve"
    - path: "main_test.go"
      provides: "테스트 케이스"
      contains: "func TestSolve"
    - path: "history/2026-W14-BOJ-1117.md"
      provides: "풀이 아카이브"
      contains: "핵심 알고리즘"
  key_links:
    - from: "main.go"
      to: "solve function"
      via: "parseInput → solve 호출"
      pattern: "solve\\("
    - from: "main_test.go"
      to: "main.go solve"
      via: "TestSolve table-driven tests"
      pattern: "solve\\("
---

<objective>
BOJ 1117 (색칠 1) 풀이를 구현하고 테스트하여 AC를 받을 수 있는 상태로 만든다.

Purpose: W×H 종이를 세로(x=f)·가로(c번) 접은 뒤 직사각형을 칠하고 펼었을 때 색칠되지 않은 면적을 구하는 문제를 Go로 해결한다.
Output: main.go (풀이), main_test.go (테스트), history/2026-W14-BOJ-1117.md (아카이브)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-boj-1117/05-CONTEXT.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: main.go I/O 스캐폴드 + solve() 구현 및 main_test.go 테스트</name>
  <files>main.go, main_test.go</files>
  <read_first>
    - main.go (현재 상태 확인 — 이전 문제 코드 덮어쓰기)
    - main_test.go (현재 상태 확인)
    - .planning/phases/05-boj-1117/05-CONTEXT.md (D-01~D-07 결정사항)
  </read_first>
  <behavior>
    - Test 1: W=10, H=10, f=1, x1=1, y1=1, x2=2, y2=2, c=0 → 기본 케이스
    - Test 2: W=10, H=10, f=3, x1=1, y1=1, x2=2, y2=2, c=1 → 접기+가로 접기
    - Test 3: W=1000000000, H=1000000000, f=500000000, x1=0, y1=0, x2=500000000, y2=500000000, c=0 → int64 오버플로 검증
    - Test 4: f=0 (접기선이 왼쪽 끝) → overlap=0, 모두 single
    - Test 5: x1=x2 → painted_x=0, 답=W*H
    - Test 6: c 최대값 테스트
  </behavior>
  <action>
    CONTEXT.md 결정사항(D-01~D-07)에 따라 구현한다.

    **main.go 구현:**
    1. 이전 문제(BOJ 25208) 코드를 완전히 교체한다.
    2. Input 구조체: W, H, f, x1, y1, x2, y2, c int64 (D-07: int64 필수)
    3. parseInput 함수: bufio.Scanner로 한 줄 읽고 fmt.Sscan으로 8개 변수 파싱
    4. solve 함수 시그니처: func solve(in Input) int64
    5. solve 로직 (per D-01~D-06):
       - overlap := min(f, W-f)  (per D-04: 겹치는 구간 = [0, min(f, W-f)])
       - doubled := max(0, min(x2, overlap) - x1)  (per D-05)
       - single := (x2 - x1) - doubled  (per D-05)
       - paintedX := doubled*2 + single  (per D-05)
       - paintedY := (y2 - y1) * (c + 1)  (per D-03: 가로 접기 c+1배)
       - return W*H - paintedX*paintedY  (per D-02)
    6. main 함수: parseInput → solve → fmt.Fprintln(writer, result)
    7. min/max 헬퍼 함수 (Go 1.21 이전 호환용, int64)

    **주의:** doubled가 음수가 되지 않도록 max(0, ...) 처리. x1 >= overlap이면 doubled=0.

    **main_test.go 구현:**
    1. TestSolve: table-driven 테스트, 최소 6개 케이스
    2. 각 케이스: Input 구조체 → solve(in) → got vs want 비교
    3. 반드시 int64 오버플로 케이스 포함 (W=H=10^9)

    **검증:** go test -v ./... 통과, golangci-lint run . 통과 (140자 줄 제한, complexity 15 이하)
  </action>
  <verify>
    <automated>go test -v ./... && golangci-lint run --allow-parallel-runners .</automated>
  </verify>
  <acceptance_criteria>
    - main.go contains `func solve(in Input) int64`
    - main.go contains `type Input struct`
    - main.go contains `func parseInput` with `bufio.Scanner`
    - main.go does NOT contain `cubeState` or `rollTable` (이전 문제 코드 완전 제거)
    - main_test.go contains `func TestSolve`
    - main_test.go contains at least 6 test case entries (table-driven)
    - `go test -v ./...` exits 0
    - `golangci-lint run .` exits 0
    - main.go uses int64 for all arithmetic (no int32 overflow)
  </acceptance_criteria>
  <done>main.go에 BOJ 1117 풀이가 구현되고, main_test.go에 6개 이상 테스트가 통과하며, lint가 통과한다</done>
</task>

<task type="auto">
  <name>Task 2: history 아카이브 작성</name>
  <files>history/2026-W14-BOJ-1117.md</files>
  <read_first>
    - .planning/phases/05-boj-1117/05-CONTEXT.md (결정사항 참조)
    - main.go (구현된 코드 확인)
    - main_test.go (테스트 결과 확인)
  </read_first>
  <action>
    history/2026-W14-BOJ-1117.md를 UTF-8 BOM으로 작성한다 (per LOG-01).

    **필수 포함 항목 (per LOG-02):**
    1. 문제 요약: W×H 종이를 세로(x=f)·가로(c번) 접은 뒤 [x1,x2]×[y1,y2] 칠하고 펼었을 때 미칠 면적
    2. 핵심 알고리즘: x/y축 독립 처리, overlap 구간 doubled×2 + single, painted_y = (y2-y1)×(c+1), 답 = W*H - paintedX*paintedY
    3. 실패/버린 아이디어: (discuss에서 논의된 대로 기록, 없으면 "직접 수학적 분석으로 도달" 등)
    4. 최종 선택 이유: x/y 독립성으로 O(1) 풀이 가능
    5. 테스트 결과: go test 통과 여부, AC 여부

    **형식:** 기존 history 파일 패턴 따르기 (# 제목, ## 섹션)
  </action>
  <verify>
    <automated>test -f history/2026-W14-BOJ-1117.md && grep -q "핵심 알고리즘" history/2026-W14-BOJ-1117.md && grep -q "테스트 결과" history/2026-W14-BOJ-1117.md</automated>
  </verify>
  <acceptance_criteria>
    - history/2026-W14-BOJ-1117.md exists
    - File contains "문제 요약" or "# BOJ 1117"
    - File contains "핵심 알고리즘"
    - File contains "실패" or "버린 아이디어"
    - File contains "선택 이유"
    - File contains "테스트 결과"
    - File is UTF-8 encoded (BOM)
  </acceptance_criteria>
  <done>history/2026-W14-BOJ-1117.md가 존재하고 풀이 요약, 핵심 알고리즘, 실패 아이디어, 선택 이유, 테스트 결과를 포함한다</done>
</task>

</tasks>

<verification>
1. `go test -v ./...` — 모든 테스트 통과
2. `golangci-lint run .` — lint 에러 없음
3. `test -f history/2026-W14-BOJ-1117.md` — history 파일 존재
4. main.go에 이전 문제(BOJ 25208) 코드가 남아있지 않음
</verification>

<success_criteria>
1. main.go가 BOJ 1117 입력을 읽고 올바른 답을 출력한다
2. main_test.go에 6개 이상의 테스트 케이스가 통과한다
3. golangci-lint 통과한다
4. history/2026-W14-BOJ-1117.md가 필수 항목을 포함한다
</success_criteria>

<output>
After completion, create `.planning/phases/05-boj-1117/05-01-SUMMARY.md`
</output>
