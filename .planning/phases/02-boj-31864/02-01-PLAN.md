---
phase: 02-boj-31864
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - main.go
  - main_test.go
  - plan.md
  - history/2026-W13-BOJ-31864.md
autonomous: false
requirements:
  - WF-01
  - WF-02
  - WF-03
  - CODE-01
  - CODE-02
  - CODE-03
  - CUR-02
  - LOG-01
  - LOG-02

must_haves:
  truths:
    - "BOJ 31864 예제 1 입력에서 solve()가 3을 반환한다"
    - "go test ./...가 PASS한다"
    - "golangci-lint run ./...가 경고·오류 없이 통과한다"
    - "history/2026-W13-BOJ-31864.md 파일이 존재한다"
    - "plan.md에 TODO 문자열이 남아 있지 않다"
  artifacts:
    - path: "main_test.go"
      provides: "BOJ 31864 예제 기반 테스트 (TestSolve)"
      contains: "solve("
    - path: "history/2026-W13-BOJ-31864.md"
      provides: "풀이 아카이브"
      contains: "GCD"
  key_links:
    - from: "main_test.go"
      to: "main.go/solve()"
      via: "직접 함수 호출"
      pattern: "solve\\("
    - from: "history/2026-W13-BOJ-31864.md"
      to: "plan.md"
      via: "풀이 요약 반영"
      pattern: "방향 정규화|GCD"
---

<objective>
BOJ 31864 "눈송이 탕후루 만들기" — 이미 main.go에 구현된 풀이를 예제 입력으로 검증하고, 테스트를 작성하고, plan.md 관찰 포인트를 마무리한 뒤 history에 아카이브한다.

Purpose: 구현은 discuss-phase에서 사용자가 직접 설계·완성했다. 이 plan은 정확성 검증 → 문서화 마무리 → 기록 보존 순서로 진행한다.
Output: 통과하는 테스트, lint-clean 코드, 완성된 plan.md, history 아카이브.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-boj-31864/02-01-CONTEXT.md

<interfaces>
<!-- main.go 현재 공개 계약. 테스트는 이 시그니처를 그대로 호출한다. -->

```go
// gcd: 음수 포함 두 정수의 최대공약수. 두 값이 0이면 0 반환.
func gcd(a, b int) int

// dir: 방향 key 타입
type dir struct{ x, y int }

// solve: N개 과일, M개 끝점을 받아 최대 꽂히는 과일 수 반환.
// 복잡도: 전처리 O(N log N), 쿼리 O(M log N)
func solve(fruits, endpoints [][2]int) int
```

예제 1 (plan.md 섹션 6에 기록됨):
- fruits: [[1,1],[2,2],[3,3],[4,4],[1,2],[2,4],[3,6],[4,8],[3,4]]
- endpoints: [[4,4],[4,0],[0,4],[3,3],[6,8]]
- 기대 출력: 3
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: main_test.go — BOJ 31864 예제 테스트 작성</name>
  <files>main_test.go</files>
  <read_first>
    - main.go (solve() 시그니처, gcd, dir 타입 확인)
    - main_test.go (기존 stub 구조 확인)
  </read_first>
  <behavior>
    - TestSolve/예제1: solve(fruits9개, endpoints5개) == 3
    - TestSolve/원점과일: gcd(0,0)==0이면 solve()가 패닉 없이 실행됨 (현재 구현 동작 확인용)
    - TestSolve/단일과일단일끝점: solve([[1,2]], [[1,2]]) == 1
    - TestSolve/방향불일치: solve([[1,2]], [[2,1]]) == 0
  </behavior>
  <action>
    main_test.go를 아래 구조로 완전히 교체한다 (기존 stub 제거):

    ```go
    package main

    import "testing"

    func TestSolve(t *testing.T) {
        tests := []struct {
            name      string
            fruits    [][2]int
            endpoints [][2]int
            want      int
        }{
            {
                name: "예제1",
                fruits: [][2]int{
                    {1, 1}, {2, 2}, {3, 3}, {4, 4},
                    {1, 2}, {2, 4}, {3, 6}, {4, 8},
                    {3, 4},
                },
                endpoints: [][2]int{{4, 4}, {4, 0}, {0, 4}, {3, 3}, {6, 8}},
                want: 3,
            },
            {
                name:      "단일과일단일끝점_일치",
                fruits:    [][2]int{{1, 2}},
                endpoints: [][2]int{{1, 2}},
                want:      1,
            },
            {
                name:      "방향불일치",
                fruits:    [][2]int{{1, 2}},
                endpoints: [][2]int{{2, 1}},
                want:      0,
            },
        }
        for _, tc := range tests {
            t.Run(tc.name, func(t *testing.T) {
                got := solve(tc.fruits, tc.endpoints)
                if got != tc.want {
                    t.Errorf("got %d, want %d", got, tc.want)
                }
            })
        }
    }
    ```

    - 외부 의존성 금지 (표준 라이브러리 testing만 사용, per CODE-03)
    - _ = t stub 제거
  </action>
  <verify>
    <automated>cd "c:/Users/김연우/Documents/dev/algo" && go test ./... -v -run TestSolve</automated>
  </verify>
  <acceptance_criteria>
    - main_test.go에 `_ = t` 가 없음: `grep -c "_ = t" main_test.go` == 0
    - main_test.go에 solve( 호출이 있음: `grep -c "solve(" main_test.go` >= 1
    - 예제1 케이스가 있음: `grep -c "예제1" main_test.go` >= 1
    - `go test ./...` 출력에 FAIL이 없음
  </acceptance_criteria>
  <done>go test ./... PASS, 예제1 포함 3개 케이스 통과</done>
</task>

<task type="auto">
  <name>Task 2: lint 검사 및 main.go 정리</name>
  <files>main.go</files>
  <read_first>
    - main.go (전체 코드 확인)
  </read_first>
  <action>
    golangci-lint run ./... 를 실행하고 지적된 항목을 수정한다.

    확인해야 할 항목:
    - 줄 길이 140자 이하 (per CODE-01)
    - cyclomatic complexity 15 이하 (per CODE-01)
    - 사용하지 않는 import 없음
    - Go 표준 라이브러리만 사용 (per CODE-03): import 블록에 bufio, fmt, os, sort 이외 패키지 금지

    lint 오류가 없으면 main.go 수정 불필요 — 결과만 기록.
    lint 오류가 있으면 최소한의 수정으로 해결하되, solve() 알고리즘 로직은 변경 금지.
  </action>
  <verify>
    <automated>cd "c:/Users/김연우/Documents/dev/algo" && golangci-lint run ./...</automated>
  </verify>
  <acceptance_criteria>
    - `golangci-lint run ./...` 종료 코드 0 (오류 없음)
    - main.go import 블록에 외부 모듈 없음: `grep -c "github.com\|golang.org/x" main.go` == 0
  </acceptance_criteria>
  <done>golangci-lint 경고·오류 0건</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: 풀이 정확성 확인 (사용자 검증)</name>
  <read_first>
    - plan.md (섹션 6 테스트 메모, 섹션 7 진행 상태)
  </read_first>
  <what-built>
    main.go의 solve(): 방향 정규화(GCD) + binary search 기반 풀이.
    main_test.go: 예제1 포함 3개 케이스 자동 테스트.
    go test ./... PASS 확인 완료.
  </what-built>
  <how-to-verify>
    1. `go test ./... -v` 실행 — 3개 케이스 모두 PASS 확인
    2. plan.md 섹션 7 "진행 상태"의 체크박스 상태 확인
    3. (선택) BOJ 31864에 코드 제출하여 AC 여부 확인
    4. 엣지 케이스 추가 확인이 필요하다고 느끼면 main_test.go에 케이스 추가 가능

    plan.md 섹션 3 "관찰 포인트"를 본인의 언어로 직접 마무리해 주세요.
    현재 내용이 충분하다고 판단되면 그대로 승인.
  </how-to-verify>
  <resume-signal>"approved" 또는 발견한 문제 설명</resume-signal>
  <acceptance_criteria>
    - 사용자가 "approved" 또는 동등한 승인 신호를 입력함
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 4: history 아카이브 작성</name>
  <files>history/2026-W13-BOJ-31864.md</files>
  <read_first>
    - history/2026-W10-BOJ-30801.md (아카이브 형식 참조)
    - AGENTS.md (history 항목 필수 요소 확인)
    - plan.md (풀이 요약, 테스트 결과)
    - .planning/phases/02-boj-31864/02-01-CONTEXT.md (핵심 결정사항)
  </read_first>
  <action>
    `history/2026-W13-BOJ-31864.md` 를 UTF-8 BOM으로 생성한다 (per LOG-01).
    언어: 한국어 (per LOG-01).

    AGENTS.md history 형식에 따라 아래 섹션을 모두 포함한다 (per LOG-02):

    ```
    # 2026-W13-BOJ-31864

    ## 문제
    - 출처 / 번호 / 이름 / 링크

    ## 요약
    - 목표, 제약, 출력

    ## 핵심 알고리즘
    - 방향 정규화: gcd(|fx|,|fy|)로 나눈 (fx/g, fy/g) pair → map key
    - 거리 비교: scale = gcd 값, 과일 scale ≤ 끝점 scale이면 포함
    - 그룹화: map[dir][]int (정렬된 scale 배열) + binary search (sort.SearchInts)
    - 복잡도: 전처리 O(N log N), 쿼리 O(M log N)

    ## 실패/버린 아이디어
    - (있으면 기록, 없으면 "없음")

    ## 최종 선택 이유
    - 사용자가 discuss-phase에서 직접 설계한 방향 요약

    ## 구현 메모
    - (0,0) 과일 처리: gcd(0,0)=0 — 구현 동작 기록
    - 반대 방향 key 분리 의도 확인됨

    ## 테스트
    - 예제 1: 9개 과일, 5개 끝점 → 3 (go test PASS)
    - 추가 케이스: ...

    ## 결과
    - AC 여부: [제출 후 업데이트]
    ```

    파일 첫 3바이트: EF BB BF (UTF-8 BOM). Write 도구로 생성 시 BOM 처리:
    content 첫 글자 앞에 U+FEFF (BOM) 문자를 포함시킨다.
  </action>
  <verify>
    <automated>cd "c:/Users/김연우/Documents/dev/algo" && ls history/2026-W13-BOJ-31864.md</automated>
  </verify>
  <acceptance_criteria>
    - `ls history/2026-W13-BOJ-31864.md` 가 파일을 반환함
    - 파일 내 "GCD" 또는 "gcd" 키워드 존재: `grep -ic "gcd" history/2026-W13-BOJ-31864.md` >= 1
    - 파일 내 "## 테스트" 섹션 존재: `grep -c "## 테스트" history/2026-W13-BOJ-31864.md` >= 1
    - 파일 내 "## 핵심 알고리즘" 섹션 존재: `grep -c "## 핵심 알고리즘" history/2026-W13-BOJ-31864.md` >= 1
  </acceptance_criteria>
  <done>history/2026-W13-BOJ-31864.md 생성, 필수 섹션 모두 포함</done>
</task>

</tasks>

<verification>
모든 태스크 완료 후 아래 명령을 순서대로 실행하여 전체 검증:

```bash
# 1. 테스트 통과
cd "c:/Users/김연우/Documents/dev/algo" && go test ./... -v

# 2. lint 통과
golangci-lint run ./...

# 3. plan.md TODO 없음
grep -c "TODO" plan.md

# 4. history 파일 존재
ls history/2026-W13-BOJ-31864.md

# 5. history 필수 섹션 확인
grep "## 핵심 알고리즘\|## 테스트\|## 결과" history/2026-W13-BOJ-31864.md
```

기대 결과:
- `go test ./...` → ok  algo [no test files 제외 후 PASS]
- `golangci-lint run` → 종료 코드 0
- `grep -c "TODO" plan.md` → 0
- history 파일 존재 및 섹션 3개 이상 출력
</verification>

<success_criteria>
- go test ./... PASS (예제1 포함 최소 3개 케이스)
- golangci-lint 경고·오류 0건
- plan.md에 TODO 문자열 없음
- history/2026-W13-BOJ-31864.md 존재, 핵심 알고리즘·테스트·결과 섹션 포함
- ROADMAP.md Phase 2 체크박스 완료 표시
</success_criteria>

<output>
완료 후 `.planning/phases/02-boj-31864/02-01-SUMMARY.md` 생성.
ROADMAP.md의 Phase 2 항목을 `[x]`로 업데이트.
STATE.md의 오늘 풀이 목록에서 BOJ 31864를 `[x]`로 업데이트.
</output>
